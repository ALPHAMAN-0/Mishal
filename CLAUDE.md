# Mishal

## Build / test / lint
- TODO: verify — no package.json/go.mod/pyproject.toml/Cargo.toml found at repo root; no build/test/lint scripts observed

## Rules
- TODO: verify — no rules found with direct evidence (no CONTRIBUTING/manifest observed in scope)

## Read first
- index.html — full page markup, script load order, editable/admin-only field markers
- assets/js/firebase-config.js — Firebase init (read before touching auth/data flow)
- README.md — repo title only

Architecture: see ARCHITECTURE.md — read before structural changes
